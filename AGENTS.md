# Nexus AI 认知进化与记忆系统说明

## 1. 系统核心文件结构 (.md 文件及其用途)

在系统运行和开发过程中，主要涉及以下几类 Markdown/文档文件：

- **`AGENTS.md` (当前文件)**: 
  - **用途**: 作为 AI 的“长期意图”存储器。它记录了系统的设计哲学、核心逻辑以及 AI 在不同阶段需要遵守的元规则。
  - **进化价值**: 每次系统架构调整或大型功能迭代，AI 都会读取并同步此文件，确保新代码不背离初始战略。

- **`DEPLOYMENT_GUIDE.md`**:
  - **用途**: 记录环境配置、Firebase 设置及生产部署步骤。
  - **进化价值**: 确保系统从开发环境平滑迁移到生产集群。

- **`security_spec.md`**:
  - **用途**: Firestore 数据安全规范。定义了哪些数据是私有的，哪些是系统生成的不可更改字段。
  - **进化价值**: 在 AI 尝试读写新数据类型时，作为“安全屏障”防止逻辑漏洞。

- **`firebase-blueprint.json` (关键元数据)**:
  - **用途**: 虽不是 .md，但它是 AI 的“骨架”。定义了所有实体（知识、客户、技能、进化提案）的结构。

---

## 2. AI 如何实现“自进化”与“记忆”？

系统的设计采用了 **四层认知模型**，实现递归式的记忆与学习：

### A. 记忆系统 (Layer 1 & 2)
- **事实记忆 (Knowledge Base)**: 存储在 `/knowledge` 集合中。当您上传文档、Excel 或输入指令时，AI 会将其“碎裂”成神经节点。查询时，AI 利用向量相似度（或全文检索）提取相关上下文，形成“基于事实的决策”。
- **关系记忆 (Client Assets)**: 存储在 `/clients/{id}/interactions`。AI 记录了与客户的每一次互动，并在“客户资产”页面生成 `memorySummary`。这意味着 AI 并不是每次都从零开始，而是带着对该客户的“历史偏见”进行思考。

### B. 认知层 (Layer 3)
- **决策路径**: 在“认知型 Agent 决策中心”，AI 会调取上述记忆，通过分析当前客户所处的 `ClientStage`（从 Phase 0 到 Phase 7），匹配最佳的战略。

### C. 技能层与自进化 (Layer 4 & Evolution)
- **技能注册表 (AgentSkill)**: 存储在 `/skills`。系统将 AI 的能力模块化（如：冷启动分析、抗拒点拆解）。
- **进化提案 (EvolutionProposal)**: 当 AI 在推理中发现现有技能无法覆盖用户需求（例如：识别到某个行业的特殊合规要求缺失），它会在后台生成一个 `EvolutionProposal`。
- **闭环迭代**: 管理员（或 AI 本身在受控状态下）批准提案后，系统会生成新的 `AgentSkill` 逻辑。这就是所谓的“自主进化”。

---

## 3. 页面显示补丁
- **智能知识库**: 已修复侧边栏统计信息在低分辨率下的溢出问题，现在三个列（对话、列表、统计）均支持独立滚动。
- **跳转逻辑**: 已实现从“控制中心”的“待执行战略行动”点击后，能自动锁定并跳转至“客户资产”中对应的具体项目。
